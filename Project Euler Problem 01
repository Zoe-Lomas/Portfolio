#this program finds the sum of multiples of 3 and/or 5 below 1000

multiples = [] #list of multiples
x = 1 #counter
while x<=1000: #makes sure counter is below 1000 so the program will end
      if x%3==0 or x%5==0: #test for multiples of 3 and/or 5
           multiples.append(x) #adds multiples to list
      x+=1 #increases counter
print(sum(multiples)) #adds the multiples and outputs the answer
